#!/bin/bash

if [ $# -eq 0 ]; then
  echo "Usage: sync-repo.sh [directory]"
  exit 1
fi

rclone sync "$1" "pkgs.omarchy.org:omarchy-pkgs/$(basename $1)" --checksum --progress --copy-links --delete-after -v
