post_install() {
  echo ">>> OpenVINO GenAI runtime installed."
  echo ">>> Shell environment is available via: source /etc/profile.d/openvino-genai.sh"
  echo ">>> (New login shells will pick it up automatically.)"
}

post_upgrade() {
  post_install
}
